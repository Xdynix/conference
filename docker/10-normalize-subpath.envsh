#!/bin/sh
# Strip trailing slash from SUBPATH before envsubst processes the nginx template.
#
# The .envsh extension and the executable bit are both load-bearing; see
# "Nginx Entrypoint Hooks" in docker/AGENTS.md.
export SUBPATH="${SUBPATH%/}"
