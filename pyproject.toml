[project]
name = "conference"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "rust-just>=1.42.3",
]
lint = [
    "pre-commit>=4.2.0",
]

[tool.uv]
default-groups = "all"
